/*
=======================================
Create Database and Schemas
=======================================
Script Purpose:
    This script create a new database named ''
    If the database exists, it is dropped and recreated. Additionally, the sscript sets up three schemas
    within the database ; 'bronze', 'silver', 'gold'. 
WARNING:
    Running this script will drop entire 'datawarehouse'database if exists.
    All the data in the database will be permanantly deleted. Proceed with caution
    and ensure yo have proper backups before running this script.
*/

USE masters;
GO

-- Drop and recreate the 'DataWarehouse' database
IF EXISTS (SELECT 1 FROM sys.databases WHERE name = 'DataWarehouse')
BEGIN
    ALTER DATABASE DataWarehouse SET SINGLE_USER WITH ROLLBACK IMMEDIATE;
    DROP DATABASE DataWarehouse;
END;
GO

-- Create the 'DataWarehouse' database
CREATE DATABASE DataWarehouse;
GO

USE DataWarehouse;
GO

--Create Schemas
CREATE SCHEMA bronze;
GO

CREATE SCHEMA silver;
GO

CREATE SCHEMA gold;
GO
